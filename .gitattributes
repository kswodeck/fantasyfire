# Normalize line endings to LF in the repo (CI runs on Linux; avoids CRLF churn).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff binary
*.woff2 binary
